# Road to 1.0 — status de hardening e trabalho restante

**Data:** 2026-05-28
**Status:** Proposto
**Base:** medido sobre `fork/fixes` (branch vivo do fork), **não** sobre `main`.
No momento da escrita, `main` está 80 commits atrás de `fork/fixes`.

---

## Nota de baseline (importante)

A primeira versão desta análise foi feita sobre o HEAD do worktree, que estava
**idêntico a `origin/main`** — 80 commits atrás de `fork/fixes`. Isso a deixou
obsoleta: a maior parte dos bugs do [issue #8](https://github.com/alkmimm/workspace-qdrant-mcp/issues/8)
e das arestas operacionais **já foi corrigida em `fork/fixes`**. Este documento
reflete o estado de `fork/fixes` e só lista o que genuinamente resta.

Regra prática para o futuro: **analisar e basear trabalho em `fork/fixes`**, não em
`main`.

---

## Snapshot de maturidade (revisado, sobre fork/fixes)

Mais maduro do que a leitura sobre `main` sugeria. Os pontos operacionais que
separavam o projeto do 1.0 — normalização de path, starvation do queue, detecção de
projeto no Windows, SQLITE_BUSY — **já foram fechados em `fork/fixes`**. A amplitude
(276k LOC Rust, MCP server TS completo, 14 workflows de CI, stack de observabilidade,
distribuição via Homebrew/Docker) continua valendo. O que segura o 1.0 agora é um
conjunto pequeno de itens de verificação, consistência e dogfooding — não capacidade.

---

## Já resolvido em fork/fixes (não refazer)

| Item original do roadmap | Como foi resolvido em fork/fixes |
|---|---|
| Duplo `tenant_id` / normalização de mount path (issue #8.1, P1.3) | `#9 fix(common): canonicalize Docker mount aliases in tenant_id calculation` — novo `src/rust/common/src/paths/docker_alias.rs` + mudança no `project_id/calculator.rs` |
| Scan de worktree para tenant órfão (issue #8.2) | `#10 fix(daemon): skip project scan when watch_folder insert is skipped for subdir` |
| Starvation no fairness scheduler (issue #8.3) | `#11 feat(daemon): age-based promotion in fairness dequeue to prevent tenant starvation` |
| `SQLITE_BUSY` / contenção no dequeue (issue #8.5) | `8f93f332c perf(fts5): batch-writer actor eliminates SQLITE_BUSY + fix UNIQUE seq collision` |
| Detecção de projeto no Windows / namespaces host↔container | `#40 bridge host/container path namespaces`, `#44 mount daemon DB + handle Windows cwd`, `#46 accept host cwd in tool body as detection fallback` |
| Branch detection / git hooks no host | `fix(hooks): set core.hooksPath`, `translate WQM_HOOK_LOG/WQM_SCRIPT for host`, métricas `wqm_git_hook_*`, testes de hook |
| Proto drift (pontual) | `#33 feat(admin): ... proto fix` (correção pontual, ver item restante abaixo) |

---

## Trabalho que ainda resta para o 1.0

### 1. Fechar / verificar o issue #8 (P0)

O issue #8 ainda está **OPEN**, mas 3 dos 4 sub-itens já caíram. Falta:

- **Sub-item #4 — "destination failure on success path" (race de meio-sucesso):** não
  teve commit dedicado; o próprio issue prevê que a correção de #1 (feita via #9) faria
  a maioria sumir. **Ação:** confirmar num stack vivo (container) que os erros em
  `batch_processing.rs` (`qdrant_status`/`search_status = failed` no caminho de sucesso)
  não recorrem. Se recorrerem no tenant sobrevivente, investigar ordem de escrita do
  status por destino.
- **Ação de fechamento:** validar #1 (não há mais duplo tenant após #9) e #4 num stack
  limpo, então **fechar ou estreitar o issue #8**.

### 2. Guard de proto drift no CI (P1)

`#33` trouxe um proto fix pontual, mas **não** o check sistemático. O proto TS é um
subconjunto mantido à mão do proto Rust; RPCs faltando compilam e só estouram em runtime
("Unknown method").

- **Ação:** adicionar um check de CI que diffa a superfície de RPC do proto TS contra o
  `.proto` canônico do daemon e falha em RPC ausente/renomeado — ou gerar o cliente TS a
  partir do proto canônico.
- **Critério:** RPC faltando é pego no CI, não em runtime.

### 3. Reconciliar versão e docs (P1, barato)

- **Versão:** código em `0.1.3` (TS + Cargo, confirmado em fork/fixes), mas existe tag
  `v0.4.1`. O guard de consistência de versão já está no CI — falta cravar um esquema
  único e reconciliar a tag solta.
- **Docs:** `CLAUDE.md` **ainda em fork/fixes** diz "v0.1.0" e "MCP Server: TypeScript
  (in development)", quando o servidor TS está completo e a versão é `0.1.3`. Varrer
  `README` e `docs/` pela mesma defasagem.
- **Critério:** tag mais recente, versões de crate e `package.json` concordam; nenhum doc
  chama de "in development" algo que já está pronto.

### 4. Robustez de runtime no hot path do daemon (P2)

Um panic no daemon derruba file watching, embedding e gRPC juntos. (Contagem de `unwrap`
fora de teste medida sobre main; **re-medir em fork/fixes**.)

- **Escopo deliberadamente estreito:** só o caminho crítico (queue processor, file
  processing, handlers gRPC, escrita de grafo). Não caçar todos os `unwrap()` do repo.
- **Critério:** sem `unwrap()`/`expect()` sobre valores falíveis (I/O, rede, parse, DB) no
  hot path; um panic em um item não aborta o loop de processamento.

### 5. Primeiros usuários / instalação limpa (P2 — maior salto de maturidade)

O projeto não tem usuários reais. Uma instalação limpa num ambiente novo
(Homebrew + Docker do zero) indexando um projeto real expõe gaps de onboarding melhor que
qualquer auditoria de código.

- **Ação:** documentar e verificar um caminho repetível "zero até a primeira busca".
- **Critério:** um usuário novo chega à primeira busca sem ler o código-fonte; gaps
  encontrados viram itens e são corrigidos.

---

## Deferido (pós-1.0)

Ver [docs/specs/14-future-development.md](../specs/14-future-development.md).

- **Graph RAG Phase 2 (LadybugDB):** Phase 1 (SQLite CTEs) já completa e batendo as metas
  de performance — não é gargalo.
- **Tagging automático (Tiers 1-3) e affinity grouping.**
- **Busca cross-project / cross-library.**
- **Roteamento por tipo de conteúdo (código vs. material de referência).**

---

## Definition of Done para o 1.0 (revisado)

- [ ] Issue #8 fechado: #4 verificado em stack vivo; #1 confirmado resolvido por #9.
- [ ] Proto drift pego no CI (não só o fix pontual de #33).
- [ ] Versões e tags reconciliadas; `CLAUDE.md`/README refletem o estado real (sem
      "v0.1.0"/"in development" defasados).
- [ ] Sem `unwrap()`/`expect()` sobre valores falíveis no hot path do daemon.
- [ ] Caminho repetível "instalação limpa → primeira busca" documentado e verificado.

---

**Relacionados:** [Issue #8](https://github.com/alkmimm/workspace-qdrant-mcp/issues/8),
[docs/plans/2026-05-25-search-quality-next-steps.md](2026-05-25-search-quality-next-steps.md),
[docs/specs/14-future-development.md](../specs/14-future-development.md),
[FIRST-PRINCIPLES.md](../../FIRST-PRINCIPLES.md)
