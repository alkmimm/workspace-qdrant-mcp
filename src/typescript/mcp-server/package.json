{
  "name": "workspace-qdrant-mcp",
  "version": "0.1.3",
  "description": "MCP server providing project-scoped Qdrant vector database operations with hybrid search",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "workspace-qdrant-mcp": "dist/index.js",
    "workspace-qdrant-agent": "dist/agent.js"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "dist/**/*.js.map",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "generate:config": "tsx scripts/generate-config-defaults.ts",
    "generate:build-info": "tsx scripts/generate-build-info.ts",
    "benchmark:semantic": "tsx scripts/benchmark-semantic-search.ts",
    "build:native": "cd ../../rust/common-node && napi build --release --platform",
    "build": "npm run clean && npm run generate:config && npm run generate:build-info && tsc && npm run copy:proto && npm run copy:admin-static && npm run chmod:bin",
    "copy:proto": "mkdir -p dist/proto && cp src/proto/*.proto dist/proto/",
    "copy:admin-static": "mkdir -p dist/admin/static && cp -R src/admin/static/. dist/admin/static/",
    "chmod:bin": "chmod +x dist/index.js dist/agent.js",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/ tests/",
    "lint:fix": "eslint src/ tests/ --fix",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "prepack": "npm run build",
    "prepublishOnly": "npm run lint && npm run test && npm run build",
    "benchmark:semantic:sweep": "TMPDIR=/tmp TMP=/tmp TEMP=/tmp tsx scripts/benchmark-semantic-search-sweep.ts"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "qdrant",
    "vector-database",
    "semantic-search",
    "claude"
  ],
  "author": "Chris",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.29",
    "@grpc/grpc-js": "^1.12.0",
    "@grpc/proto-loader": "^0.7.13",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@qdrant/js-client-rest": "^1.17.0",
    "better-sqlite3": "^11.7.0",
    "pino": "^9.6.0",
    "pino-roll": "^4.0.0",
    "prom-client": "^15.1.3",
    "yaml": "^2.6.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.0",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
